<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edwing123 Blog | <%= title %></title>
  <link
    href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap"
    rel="stylesheet"
  />
  <style>
    *,
    *::before,
    *::after {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      font-size: 62.5%;
      font-family: "Montserrat", sans-serif;
      --header-padding: 2rem;
      --brand-font-size: 2rem;
      --navigation-liks-font-size: 1.8rem;
      --footer-padding: 2rem;
      --footer-paragraph-font-size: 1.5rem;
      /* text-size-adjust: none; */
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: var(--header-padding);
      background-color: #0097e6;
    }

    .brand-logo a {
      text-decoration: none;
      font-size: var(--brand-font-size);
      color: white;
      font-weight: 600;
    }

    .navigation-menu {
      list-style-type: none;
      display: flex;
      justify-content: space-between;
    }

    .navigation-menu li {
      margin: 0 .6rem;
    }

    .navigation-menu a {
      text-decoration: none;
      color: white;
      font-size: var(--navigation-liks-font-size);
    }

    .navigation-menu a:focus, .navigation-menu a:hover, .navigation-menu .active-link {
      text-decoration: underline;
    }

    .main {
      margin: 2rem 0 0;
      min-height: calc(
      100vh 
      - (2 * var(--header-padding)) 
      - var(--brand-font-size)
      - var(--footer-paragraph-font-size)
      - (2 * var(--footer-padding))
      - 2rem
      );
    }

    .section {
      padding: 2rem;
    }

    .section h2 {
      font-size: 2.5rem;
      margin: 0 0 2rem;
    }

    .section p {
      font-size: 1.9rem;
    }

    .blogs {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      grid-auto-rows: min-content;
      gap: 1.2rem;
    }

    .blog-card {
      background: linear-gradient(to right bottom, #5f27cd, #7431fc);
      padding: 1.2rem;
      color: #ffffff;
    }

    .blog-title {
      font-size: 1.8rem;
      font-weight: 600;
      margin: 0 0 1.2rem;
      cursor: pointer;

      text-overflow: ellipsis;
      white-space: nowrap;
      overflow: hidden;
    }

    .blog-snippet {
      font-size: 1.5rem;
    }

    .footer {
      background-color: #485460;
      padding: var(--footer-padding);
    }

    .footer-copyright {
      color: white;
      font-size: var(--footer-paragraph-font-size);
    }

    .alert-not-found {
      width: 90%;
      max-width: 400px;
      text-align: center;
      font-size: 3rem;

      margin: 2rem auto 0;
      padding: 1rem;
      border-radius: .5rem;
      background-color: #eb3b5a;
      color: #ffffff;
    }

    .blog-editor {
      background: linear-gradient(to right bottom, #5f27cd, #7431fc);
      max-width: 600px;
      border-radius: .5rem;
      padding: 2rem;
      margin: 4rem auto;
    }

    .form-label {
      display: block;
      margin: 0 0 1.4rem;
      color: #ffffff;
      font-size: 1.8rem;
    }

    .form-label input, .form-label textarea {
      display: block;
      width: 100%;
      max-width: 100%;
      border: none;
      border-radius: .4rem;
      padding: .8rem 1rem;
      margin: .5rem 0 0;
      color: #24292e;
      font-size: 1.8rem;
    }

    .form-label textarea {
      height: 130px;
      max-height: 130px;
    }

    .form-button {
      display: inline-block;
      background: #ecf0f1;
      padding: .8rem 1rem;
      border: none;
      border-radius: .5rem;
      cursor: pointer;
      font-size: 1.6rem;
    }
  </style>
</head>
